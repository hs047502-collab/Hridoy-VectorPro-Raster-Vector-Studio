# VectorPro Autonomous

GitHub Pages-ready build. Open `index.html` locally or publish this folder with GitHub Pages.

The app uses its built-in VTracer/WebAssembly loading logic and CDN fallback, so no separate WASM file is required for the current build.
